# 霊宝コレクション — アイテム一覧・出現率

> 数値はすべてサーバー権威。出典: [server/src/rooms/mmo/MmoRoom.ts](../../server/src/rooms/mmo/MmoRoom.ts) の `ITEMS` / `EQUIP` 定義。
> 値を変えたら本書も更新すること。更新日: 2026-06-08

---

## 1. アイテム一覧

| ID | 名称 | 種別 | 効果 | 価格(購入) | 売値 |
|---|---|---|---|---|---|
| `potion_s`   | 回復薬     | 回復(heal)      | HPを **60** 回復              | 🪙30 | 🪙12 |
| `potion_l`   | 上級回復薬 | 回復(heal)      | HPを **180** 回復             | 🪙80 | 🪙32 |
| `elixir_atk` | 力の薬     | バフ(buffAtk)   | **30秒** 攻撃 **×1.5**        | —    | 🪙16 |
| `elixir_spd` | 俊足の薬   | バフ(buffSpeed) | **30秒** 移動速度 **×1.4**    | —    | 🪙16 |
| `scroll_atk` | 力の巻物   | 恒久(perm)      | 攻撃力 **+3**（永続）         | —    | 🪙80 |
| `scroll_def` | 堅の巻物   | 恒久(perm)      | 防御力 **+1**（永続）         | —    | 🪙80 |
| `scroll_hp`  | 生命の巻物 | 恒久(perm)      | 最大HP **+20**（永続）        | —    | 🪙80 |

- **回復薬／上級回復薬**: ショップ購入のみ（ドロップしない）。
- **バフ薬（力の薬／俊足の薬）**: **宝箱から**入手（討伐ドロップしない）。→ §3
- **巻物**: 討伐ドロップのみ（ショップ販売なし）。
- 巻物の恒久ボーナスは `bonusAtk / bonusDef / bonusMaxHp` として永続化され、レベル由来のステータスに加算される。

---

## 2. 討伐ドロップ（巻物のみ）

討伐ドロップで出るのは **巻物のみ**。バフ薬は宝箱（§3）から。

1. **ドロップ判定**: 討伐ごとに `SCROLL_DROP_CHANCE = 0.67%` でドロップ有無を抽選。
2. **品目抽選**: 当たったら、巻物3種を均等（重み各1）にランダム抽選。

| アイテム | 計算 | 実効率 | 目安（1個出るまで） |
|---|---|---|---|
| 力の巻物   | 0.67% × 1/3 | **約 0.22%** | 約 450 討伐 |
| 堅の巻物   | 0.67% × 1/3 | **約 0.22%** | 約 450 討伐 |
| 生命の巻物 | 0.67% × 1/3 | **約 0.22%** | 約 450 討伐 |
| 巻物いずれか | 0.67% | 約 0.67% | 約 150 討伐 |

> 階層・敵種別によるドロップ率の変動はなし（一律 0.67% 判定）。

---

## 3. 宝箱（バフ薬の入手元）

狩場のフィールドに湧く宝箱を **[E]キー** で開けると、**バフ薬1個＋ゴールド**を入手。

| 項目 | 値 |
|---|---|
| 出現場所 | 狩場のみ（町には出ない） |
| 同時出現数 | 常時 **2個**（`TREASURE_TARGET`） |
| 補充間隔 | 約 **12秒**（`TREASURE_SPAWN_COOLDOWN_MS`） |
| 開封 | 近接（48px以内）で `[E]`。サーバー権威で距離検証 |
| 中身（バフ薬） | 力の薬 / 俊足の薬 を **均等ランダムで1個** |
| 中身（ゴールド） | `30 + 階層×20 + 0〜19`（深層ほど多い） |

> 宝箱は誰でも開封可（ゴールド／バフ薬は有限供給ではない）。開封時のバフ薬・ゴールドはログイン者のみ永続化。

---

## 4. ゴールド獲得（購入の元手）

討伐ごとに獲得。出典: `onMobKilled`。

```
獲得G = max(1, round((2 + floor × 2) × 敵種別のexpMul))
```

- `floor` = 現在の階層（町=0、狩場 B1〜B5＝1〜5）。深層ほど多い。
- `expMul` = 敵種別ごとの倍率（ボス等は高い）。
- このほか宝箱開封でも入手（§3）。

---

## 5. 入手・消費まとめ

| アイテム | 入手 | 消費/使用 |
|---|---|---|
| 回復薬 / 上級回復薬 | 町ショップで購入（🪙30 / 🪙80） | 使用でHP回復（満タン時は消費しない） |
| 力の薬 / 俊足の薬 | **宝箱**（狩場・[E]で開封） | 使用で30秒バフ |
| 力/堅/生命の巻物 | 討伐ドロップ | 使用で恒久ステータスアップ（アイテムタブからクリック使用） |
| 全アイテム | — | 町ショップ「売却」タブでゴールド化 |

> 巻物は誤爆防止のためホットキー（数字1〜4）対象外。アイテムタブからクリックで使用。

---

## 6. 装備（無限供給・アイテム型）

装備は霊宝の保存則(INV-1〜6)とは**無関係の無限供給**。出典: `EQUIP` 定義。
6スロット（武器/盾/頭/鎧/護符/指輪）。所持(未装備)は `gear`、装備中は `equip`（slot→equipId）で永続化。
装備中ギアの補正は `recalcStats` で `atk/def/maxHp` に加算される（巻物ボーナスと同列）。

| ID | 名称 | スロット | 効果 | 価格(購入) | 売値 | ドロップ |
|---|---|---|---|---|---|---|
| `sword_wood`    | 木の剣     | 武器(weapon) | 攻撃 **+3**            | 🪙60  | 🪙24 | — |
| `sword_iron`    | 鉄の剣     | 武器(weapon) | 攻撃 **+8**            | 🪙200 | 🪙80 | あり |
| `shield_wood`   | 木の盾     | 盾(shield)   | 防御 **+2**            | 🪙60  | 🪙24 | — |
| `armor_leather` | 革の鎧     | 鎧(armor)    | 防御 **+2** / 最大HP **+20** | 🪙120 | 🪙48 | — |
| `amulet_vigor`  | 活力の護符 | 護符(amulet) | 最大HP **+40**         | 🪙150 | 🪙60 | あり |
| `ring_power`    | 力の指輪   | 指輪(ring)   | 攻撃 **+5**            | 🪙180 | 🪙72 | あり |

- **入手**: 町ショップ「装備」タブで購入 ／ 討伐の低確率ドロップ（`EQUIP_DROP_CHANCE = 0.5%`、`dropWeight>0` の3種を均等抽選）。
- **装備/解除**: ステータス画面(C)の「装備」タブ。所持装備クリックで装着、スロットクリックで解除。同スロットの既装備は所持へ戻る。
- **売却**: ショップ「売却」タブ（未装備の `gear` 分のみ。装備中は外してから）。
- 頭(head)スロットは枠のみ（対応装備は未設定）。

> 数値・効果はバランス調整前の仮値。調整は [TODO.md](TODO.md) を参照。

---

## 7. 関連

- バランス調整は [TODO.md](TODO.md) の「運用・調整」を参照。
- 防御ステータス（被ダメ計算）は割合軽減: `被ダメ = 敵atk × 100/(100+def)`、最小1。
